# CALCULADOR CLI


## Instruções

Executar a aplicar com uv:

```sh
uv run calculator
```

Executar a aplicação com Docker

```sh
docker run -it calculator:latest
```

Executar testes

```sh
uv run pytest
```

Executar linter

```sh
uv run ruff check
```
